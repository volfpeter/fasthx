site_name: FastHX
repo_name: volfpeter/fasthx
repo_url: https://github.com/volfpeter/fasthx

theme:
  name: modern
  palette:
    # Palette toggle for automatic mode
    - media: "(prefers-color-scheme)"
      scheme: slate
      toggle:
        icon: lucide/sun-moon
        name: Switch to light mode
    # Palette toggle for light mode
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle:
        icon: lucide/sun
        name: Switch to dark mode
    # Palette toggle for dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle:
        icon: lucide/moon
        name: Switch to system preference
  features:
    - navigation.indexes
    - navigation.instant
    - navigation.tracking
    - search.suggest
    - search.highlight

plugins:
  - search:
      lang: en
  - mkdocstrings:
      handlers:
        python:
          paths: [.]

markdown_extensions:
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
      pygments_lang_class: true
  - pymdownx.inlinehilite
  - pymdownx.snippets
  - pymdownx.superfences

nav:
  - index.md
  - Examples:
      - Examples: examples/index.md
      - examples/htmy.md
      - examples/htmy-streaming.md
      - examples/jinja-templating.md
      - examples/custom-templating.md
  - API Reference:
      - api/core-decorators.md
      - api/component_selectors.md
      - htmy: api/htmy.md
      - api/jinja.md
      - api/dependencies.md
      - api/typing.md
  - Migrations:
      - migrations/2-to-3.md
      - migrations/1-to-2.md
